trigger: none
pr: none

parameters:
- name: environment
  displayName: Target Environment
  type: string
  default: dev
  values:
  - dev
  - sit
  - uat
  - prod

- name: terraformRoot
  displayName: Project Terraform Directory
  type: string
  default: infra\app
  values:
  - infra\app
  - infra\network
  - infra\data
  - infra\shared

- name: operation
  displayName: Operation
  type: string
  default: deploy
  values:
  - deploy
  - delete

- name: runPlan
  displayName: Run Terraform Plan
  type: boolean
  default: true

- name: runApply
  displayName: Execute Apply/Destroy
  type: boolean
  default: false

variables:
# =========================
# Environment -> Service Connection / Subscription / tfvars
# =========================
- ${{ if eq(parameters.environment, 'dev') }}:
  - name: serviceConnection
    value: 'sc-terraform-dev'
  - name: subscriptionId
    value: '11111111-1111-1111-1111-111111111111'
  - name: tfVarsFile
    value: 'env\dev.tfvars'
  - name: stateKeyPrefix
    value: 'dev'

- ${{ if eq(parameters.environment, 'sit') }}:
  - name: serviceConnection
    value: 'sc-terraform-sit'
  - name: subscriptionId
    value: '22222222-2222-2222-2222-222222222222'
  - name: tfVarsFile
    value: 'env\sit.tfvars'
  - name: stateKeyPrefix
    value: 'sit'

- ${{ if eq(parameters.environment, 'uat') }}:
  - name: serviceConnection
    value: 'sc-terraform-uat'
  - name: subscriptionId
    value: '33333333-3333-3333-3333-333333333333'
  - name: tfVarsFile
    value: 'env\uat.tfvars'
  - name: stateKeyPrefix
    value: 'uat'

- ${{ if eq(parameters.environment, 'prod') }}:
  - name: serviceConnection
    value: 'sc-terraform-prod'
  - name: subscriptionId
    value: '44444444-4444-4444-4444-444444444444'
  - name: tfVarsFile
    value: 'env\prod.tfvars'
  - name: stateKeyPrefix
    value: 'prod'

# =========================
# Remote state config
# =========================
- name: tfStateResourceGroup
  value: 'rg-tfstate'
- name: tfStateStorageAccount
  value: 'satfstate001'
- name: tfStateContainer
  value: 'tfstate'

pool:
  name: 'Your-Windows-Agent-Pool'

stages:
- stage: Terraform
  displayName: Terraform_${{ parameters.environment }}_${{ parameters.operation }}
  jobs:
  - job: TerraformRun
    displayName: Validate_Plan_Execute
    steps:
    - checkout: self

    - powershell: |
        Write-Host "Environment        : ${{ parameters.environment }}"
        Write-Host "Terraform Root     : ${{ parameters.terraformRoot }}"
        Write-Host "Operation          : ${{ parameters.operation }}"
        Write-Host "Run Plan           : ${{ parameters.runPlan }}"
        Write-Host "Run Apply          : ${{ parameters.runApply }}"
        Write-Host "Service Connection : $(serviceConnection)"
        Write-Host "Subscription Id    : $(subscriptionId)"
        Write-Host "tfvars file        : $(tfVarsFile)"
      displayName: Show runtime selections

    - powershell: |
        $ErrorActionPreference = 'Stop'
        terraform version
        az version
      displayName: Check Terraform and Azure CLI

    - task: AzureCLI@2
      displayName: Validate / Plan / Execute Terraform
      inputs:
        azureSubscription: '$(serviceConnection)'
        scriptType: 'ps'
        scriptLocation: 'inlineScript'
        addSpnToEnvironment: true
        inlineScript: |
          $ErrorActionPreference = 'Stop'

          $tfRoot      = '${{ parameters.terraformRoot }}'
          $tfVarsFile  = '$(tfVarsFile)'
          $operation   = '${{ parameters.operation }}'
          $runPlan     = '${{ parameters.runPlan }}'
          $runApply    = '${{ parameters.runApply }}'

          if (-not (Test-Path $tfRoot)) {
            throw "Terraform root directory not found: $tfRoot"
          }

          if (-not (Test-Path (Join-Path $tfRoot $tfVarsFile))) {
            throw "tfvars file not found: $tfRoot\$tfVarsFile"
          }

          az account set --subscription "$(subscriptionId)"
          Write-Host "Using Azure account:"
          az account show

          # Map service connection identity to Terraform Azure auth env vars
          $env:ARM_CLIENT_ID       = $env:servicePrincipalId
          $env:ARM_CLIENT_SECRET   = $env:servicePrincipalKey
          $env:ARM_TENANT_ID       = $env:tenantId
          $env:ARM_SUBSCRIPTION_ID = "$(subscriptionId)"

          $safeTfRoot = $tfRoot.Replace('\', '-').Replace('/', '-')
          $tfStateKey = "$(stateKeyPrefix)-$safeTfRoot.tfstate"

          Write-Host "Terraform init..."
          terraform -chdir="$tfRoot" init `
            -backend-config="resource_group_name=$(tfStateResourceGroup)" `
            -backend-config="storage_account_name=$(tfStateStorageAccount)" `
            -backend-config="container_name=$(tfStateContainer)" `
            -backend-config="key=$tfStateKey"

          Write-Host "Terraform format check..."
          terraform -chdir="$tfRoot" fmt -check -recursive

          Write-Host "Terraform validate..."
          terraform -chdir="$tfRoot" validate

          if ($runPlan -eq "True" -or $runPlan -eq "true") {
            if ($operation -eq "deploy") {
              Write-Host "Running terraform plan for deploy..."
              terraform -chdir="$tfRoot" plan `
                -var-file="$tfVarsFile" `
                -out=tfplan
            }
            elseif ($operation -eq "delete") {
              Write-Host "Running terraform plan for destroy..."
              terraform -chdir="$tfRoot" plan `
                -destroy `
                -var-file="$tfVarsFile" `
                -out=tfplan-destroy
            }
            else {
              throw "Unsupported operation: $operation"
            }
          }

          if ($runApply -eq "True" -or $runApply -eq "true") {
            if ($operation -eq "deploy") {
              Write-Host "Executing terraform apply..."
              if (-not (Test-Path (Join-Path $tfRoot 'tfplan'))) {
                throw "Plan file tfplan not found. Please run plan first."
              }
              terraform -chdir="$tfRoot" apply -auto-approve tfplan
            }
            elseif ($operation -eq "delete") {
              Write-Host "Executing terraform destroy..."
              if (Test-Path (Join-Path $tfRoot 'tfplan-destroy')) {
                terraform -chdir="$tfRoot" apply -auto-approve tfplan-destroy
              }
              else {
                terraform -chdir="$tfRoot" destroy `
                  -auto-approve `
                  -var-file="$tfVarsFile"
              }
            }
            else {
              throw "Unsupported operation: $operation"
            }
          }